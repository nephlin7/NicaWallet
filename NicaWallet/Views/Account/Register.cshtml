@model NicaWallet.Models.RegisterViewModel
@{
    Layout = "~/Views/Shared/_LayoutAccount.cshtml";
}

<section class="login-section">
    <div class="container">
        <div class="row">
            <div class="login-wrapper">
                <div class="login-inner">

                    <div class="logo">
                        <img src="~/NicaWallet2.png" alt="logo" />
                    </div>

                    @using (Html.BeginForm("Register", "Account", FormMethod.Post, new { @class = "form-horizontal", role = "form" }))
                    {
                        @Html.AntiForgeryToken()
                        <div class="form-group">
                            @Html.TextBoxFor(m => m.Email, new { @class = "form-control", @placeholder = "Username" })
                        </div>

                        <div class="form-group">
                            @Html.TextBoxFor(m => m.Password, new { @class = "form-control", @placeholder = "Password" })
                        </div>

                        <div class="form-group">
                            @Html.PasswordFor(m => m.ConfirmPassword, new { @class = "form-control", @placeholder = "Confirm Password" })
                        </div>                        

                        <div class="form-group">
                            <input type="submit" value="Sign Up" class="btn btn-primary btn-block">
                        </div>

                        <div class="form-group text-center">
                            Already have an account?  @Html.ActionLink(" Sign In", "Login")
                        </div>

                    }

                    <div class="copy-text">
                        <p class="m-0">2017 &copy; NicaWallet</p>
                    </div>

                </div>
            </div>

        </div>
    </div>
</section>
